name: barter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_screenutil: ^5.9.3
  google_fonts: ^6.3.2
  flutter_svg: ^2.2.1
  intl: ^0.20.2
  flutter_localizations:
    sdk: flutter
  provider: ^6.1.5+1
  shared_preferences: ^2.5.3
  fluttertoast: ^8.2.8  # Downgraded to resolve conflict
  google_sign_in: ^6.2.2
  image_picker: ^1.1.0
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  jwt_decoder: ^2.0.1
  signalr_netcore: ^1.3.5
  http: ^1.1.0
  package_info_plus: ^4.2.0
  share_plus: ^7.2.1
  google_maps_flutter: ^2.14.0
  geocoding: ^4.0.0
  location: ^8.0.1
  geolocator: ^14.0.1
  url_launcher: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/